<% if (BowserBook.CurrentUser.id === parseInt(userId)) { %>
  <h4>Friend Requests</h4>
  <% if (numFriendRequests === 0) { %>
    <h5>No new friend requests</h5>
  <% } %>
  <ul class="friend-requests-list clearfix"></ul>
<% } %>